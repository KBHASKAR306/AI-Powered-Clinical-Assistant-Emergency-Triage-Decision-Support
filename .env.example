# Copy this file to .env and fill in your key to use real Claude reasoning.
# If left unset, all pattern reasoning steps fall back to a deterministic
# offline heuristic (see app/llm.py) -- the whole demo still runs and all
# tests still pass with no key configured.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6
